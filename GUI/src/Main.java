public class Main {
    public static void main(String[] args) {
        //MyFrame myFrame= new MyFrame();
        new MyFrame();

        //we can use the above method only by writing
        // that new Frame()
        // but this is only for a case where this frame is not to be edited
    }
}
